@page "/sync"
@inject NavigationManager Nav

@* Sync moved into Settings — keep the old route working for bookmarks/links. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("settings", replace: true);
}
